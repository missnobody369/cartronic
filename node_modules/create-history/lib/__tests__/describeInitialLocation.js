'use strict';

exports.__esModule = true;

var _expect = require('expect');

var _expect2 = _interopRequireDefault(_expect);

var _Actions = require('../Actions');

var _execSteps = require('./execSteps');

var _execSteps2 = _interopRequireDefault(_execSteps);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var describeInitialLocation = function describeInitialLocation(createHistory) {
  describe('on the initial POP', function () {
    var history = void 0;
    beforeEach(function () {
      history = createHistory();
    });

    it('location does not have a key', function (done) {
      var steps = [function (location) {
        (0, _expect2.default)(location.action).toEqual(_Actions.POP);
        (0, _expect2.default)(location.key).toNotExist();
      }];

      (0, _execSteps2.default)(steps, history, done);
    });
  });
};

exports.default = describeInitialLocation;