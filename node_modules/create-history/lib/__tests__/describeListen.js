'use strict';

exports.__esModule = true;

var _expect = require('expect');

var _expect2 = _interopRequireDefault(_expect);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var describeListen = function describeListen(createHistory) {
  var history = void 0,
      unlisten = void 0;
  beforeEach(function () {
    history = createHistory();
  });

  afterEach(function () {
    if (unlisten) unlisten();
  });

  describe('listen', function () {
    it('does not immediately call listeners', function () {
      var spy = _expect2.default.createSpy();
      unlisten = history.listen(spy);
      (0, _expect2.default)(spy).toNotHaveBeenCalled();
    });
  });
};

exports.default = describeListen;