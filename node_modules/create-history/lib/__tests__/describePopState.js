'use strict';

exports.__esModule = true;
var describePopState = function describePopState(createHistory) {
  describe('when a listenBefore hook is added', function () {
    var history = void 0,
        unlisten = void 0;
    beforeEach(function () {
      history = createHistory();
      history.push('/home');
    });

    afterEach(function () {
      if (unlisten) unlisten();
    });

    it('is called when browser navigation is used', function (done) {
      unlisten = history.listenBefore(function () {
        done();
      });

      window.history.back();
    });
  });
};

exports.default = describePopState;